'use client'

import { useState } from 'react';

const GroupAnagrams = () => {
    //const [anagramObject, setAnagramObject] = useState({});
    //generate a list of strings?
    return (
        <div className="pt-4">
            some things to return
        </div>
    );
}

export default GroupAnagrams;